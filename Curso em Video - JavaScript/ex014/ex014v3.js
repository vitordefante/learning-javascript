valores = [6, 8, 6, 5, 4, 2, 9, 0, 5]

for(let pos in valores){
    console.log(valores[pos])
} // looping com sintaxe similar a do python, muda apenas a maneira de declarar a variavel, que necessita o tipo
